package ${package};

/**
 * Placeholder so a freshly generated project has something to compile, test and document out of the box.
 *
 * <p>Delete this class and {@code PlaceholderTest} once the library has its own public API - javadoc
 * generation fails on a package that contains only {@code package-info.java} and no actual class.
 */
public final class Placeholder {

    public String describe() {
        return "Replace me with ${artifactId}'s actual API.";
    }
}
